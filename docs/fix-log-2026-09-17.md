# Fix log — file bugs F1–F12 (17 September 2026)

Applied by Claude (Cowork) against `C:\Users\Gebruiker\ableton-mcp`, working
from the bug register in
`docs/Suno_v6_Parity_Ableton_MCP_Evaluation_2026-09-17.docx`.
No Ableton session was touched; no git command was run (see "Left for you").

## Verification

- `pytest tests/` — **112 passed** (95 before + 17 new regression tests).
- `compileall` on `MCP_Server`, `tests`, `AbletonMCP_Remote_Script`,
  `music-os-bridge/_howto.py` — clean.
- `ruff check --select F401,F541` on the maintained tree — clean.
- Both Remote Script copies verified byte-identical after patching.
- Run locally as: `python -m pytest tests -q` (Python 3.13 venv, `mcp<2`).

## Fixed

| ID | Fix |
|----|-----|
| F1 | Implemented the missing `_get_browser_categories` and `_get_browser_items` handlers (both were dispatched at line 427/430 and raised `AttributeError`). An AST test now asserts every `self.*` handler the dispatcher routes actually exists. |
| F2 | `_add_notes_to_clip` now **requires** `start_time` (accepts `start_beat` as an alias) and rejects non-numeric fields, negative starts and non-positive durations, naming the offending note index. No more silent beat-zero stacking. |
| F3 | Added a `save_set` command: new main-thread handler `_save_set`, advertised capability, and a `save_set` MCP tool in `server.py`. It probes the save APIs Live may expose (`song.save_set`, `document.save_set`, `app.save_set`) and returns a typed `save_unsupported` refusal instead of a false success when none exists. |
| F4 | `_create_locator` rewritten: accepts Live snapping the stopped playhead (takes the cue that actually appeared within one beat and reports `requested_time`, `time`, `snapped`), always restores the playhead, and raises an error naming the transport state and where the playhead landed. |
| F6 | `.gitignore`: all audio/project/archive extensions, `suno-harvest/`, `evaix-bridge/samples/`, `evaix-bridge/renders/`, `_to_delete/`, `_local/`. Keeps ~15 GB out of git. |
| F7 | Added `.gitattributes` (`* text=auto eol=lf`, CRLF kept for `.ps1`/`.cmd`/`.bat`, audio marked binary, `uv.lock` `-diff`). Run `git add --renormalize .` once to clear the phantom modifications. |
| F8 | `music-os-bridge/_howto.py` — the Windows path inside the literal made it a `SyntaxError`; the literal is now raw and the file parses. |
| F9 | `find_vps_pass.py`, `find_vps_pass2.py`, `query_opencode_db.py`, `query_opencode_db2.py` moved to `_local/` (gitignored, with a README). |
| F10 | The five placeholder-free f-strings in `server.py` fixed. |
| F12 | `ruff --fix` removed 10 unused imports; `MCP_Server/__init__.py` now declares `__all__` for its intentional re-exports. |
| — | `SCRIPT_VERSION` and `EXPECTED_REMOTE_SCRIPT_VERSION` bumped **1.7.0 → 1.7.1** together, and the three new commands added to `SCRIPT_CAPABILITIES`. |

New tests live in `tests/test_remote_script_contract.py` (stubs `_Framework`,
no Ableton and no network required).

## Left for you

1. **Deploy the script and restart Live.** The loaded script still reports
   1.7.0, so the handshake will call it stale until Live reloads:
   `uv run python -m MCP_Server.remote_script_install --force`, then restart
   Live and confirm `get_script_info` reports 1.7.1 with a full
   `ableton_mcp_snapshot_v2`. This is also the moment to settle **F5** — pick
   the 1.7.0 family deliberately and replace the 1.8.0 file in ProgramData,
   capturing one real `get_session_info` response per version as a fixture
   first.
2. **Commit (F13).** Nothing was committed. Suggested order, after
   `git add --renormalize .`: Remote Script + install + tests, then
   `server.py`, then `.gitignore`/`.gitattributes`, then docs. Add a
   `gitleaks` pre-commit hook before the first push.
3. **Prune the stale worktree (F11).** `git worktree prune` (the Cowork shell
   cannot delete files on your machine, so `.claude/worktrees/agent-issues-debug-dda3ed`
   is still there).
4. **Delete `_to_delete/git-index.lock.empty`** — an empty `.git/index.lock`
   left by a read-only git status; moving it there unblocked committing.
5. **F14:** audition the pending A/B run `cdf96e15…` at matched loudness and
   record the verdict with `producer_remember` before building on it.
6. `evaix-bridge` lint (F12 remainder) — 65 unused variables and 40 empty
   f-strings in one-off build scripts were left alone on purpose; archive
   those scripts rather than fixing them.

---

# Part 2 — Suno v6 benchmark and pipeline scaffolding (same day)

New, all covered by tests that need neither Ableton nor audio hardware
(`pytest tests/` — **167 passed**, 55 of them new):

- `docs/SUNO_V6_BENCHMARK.md` — the quality bar: reference-set rules and the
  approval gate, hard rules (true-peak ceiling, low-end mono and phase, kick
  owns 30–120 Hz), reference rules from a profile, the blind loudness-matched
  A/B protocol with parity at ≤60% preference for the Suno reference, the
  tuning loop, and what the gate does not claim. Linked from `PRODUCER.md`.
- `MCP_Server/producer/ear/` — the mix ear: shared band edges (pinned by test
  to `producer.audio`), true peak (4× oversampled estimate), integrated LUFS
  via `pyloudnorm` when installed (otherwise `None` plus a reason, which
  blocks a pass), band energy, low-end mono/phase, stereo width, section
  energy map, kick detection and between-kick sub ownership, reference
  profiles, and the QC gate.
- `MCP_Server/producer/pipeline/` — section maps as YAML/JSON data (exactly
  one `sub_owner` per section, validated), resumable per-build state with
  stage ordering, and stages: `intake` (hash + header inventory), `align`
  (tempo/downbeat estimates with confidence), `qc` (measure, gate, write a
  report), `publish` (standard result message, refuses unless QC passed).
  `build`, `mix` and `master` raise `StageNeedsLive` pointing at pre-flight
  rather than pretending to work headless. Example map:
  `pipeline/sections.example.yaml`.
- `MCP_Server/producer/preflight.py` — bridge reachable, script version as
  expected, full v2 snapshot contract, exactly one Live instance, disk
  headroom. Read-only.
- New MCP tools: `ear_scan`, `ear_kick_bass`, `ear_qc`,
  `ear_build_reference_profile`, `producer_preflight`, `pipeline_status`.
- New CLI commands: `ear`, `qc`, `refprofile`, `preflight`, `sections`,
  `pipeline {status,intake,align,qc,publish}`.
- `pyproject.toml` — `PyYAML` and `pyloudnorm` added to the `producer` extra,
  `PyYAML` to `test`; the two new subpackages and the example YAML packaged.

Verified end to end offline on synthetic audio: a render with a continuous
sub bass under the kick fails `sub_ownership` (−2.7 dB between-kick vs kick)
and the true-peak ceiling; the clean reference passes the same profile; the
tempo estimate returns 162.2 BPM on a 162 BPM click; state resumes at `build`.

## Next steps for this part

1. `pip install -e '.[producer,test]'` (or `uv sync`) to pick up PyYAML and
   pyloudnorm — without pyloudnorm the LUFS checks stay unmeasured and no
   render can pass the gate.
2. With Hermes approval, gather the Suno v6 reference set, then
   `refprofile ... --save-to .producer/profiles/suno-v6-acidcore.json`.
3. Re-run QC on EvAIx_HarvestMix_162 to confirm the gate reproduces the clash
   you heard, then fix it per section with sidechain and sub ownership.
4. Build the 162 BPM default Live Set (master limiter, bass mono, high-passed
   stems) — the API cannot reach the master track, so it has to live in the
   template.
5. Wire `build`/`mix`/`master` to the producer engine once the bridge family
   is settled and pre-flight passes on the PC.

---

# Part 3 — executed on the PC, 18 September 2026

Live was not running, so nothing was open to lose.

## Done

- **Dependencies:** `pip install -e ".[producer,test]"` into `.venv`
  (Python 3.12.14): numpy 2.3.5, PyYAML, pyloudnorm 0.1.1, pytest 9.1.1.
  Integrated LUFS is therefore measured, not skipped.
- **Tests on the PC: 167 passed** in the project venv, and again after the
  script deploy. One environment quirk: with the default temp root (the 8.3
  short path `C:\Users\GEBRUI~1\AppData\Local\Temp`) every `tmp_path` test
  fails with `PermissionError`; run
  `pytest tests --basetemp=_local/pytest-tmp` on this machine. Recorded as a
  comment in the new `[tool.pytest.ini_options]` block.
- **Committed in 8 logical commits** (working tree now clean, `main` at
  `231486b`): git hygiene; the bridge fixes (1.7.1); the server fixes; the
  15–16 September audit fixes; the producer ear/QC/pipeline/pre-flight; docs;
  the archived experiments; and the evaix/music-os bridge scripts.
  **Zero audio files are tracked** (`git ls-files` matches none).
  Local identity set to `Ryan PDS <ryantordeurs@outlook.com>` — the global
  identity was `GitHub Copilot <copilot@github.com>`, which would have
  authored these commits otherwise.
- **Pre-commit guard** added at `.githooks/pre-commit` (blocks audio and
  credential-shaped strings, no external tools) and wired with
  `git config core.hooksPath .githooks`. It could not execute in this
  session — git's bundled `sh` cannot fork here (`0xC0000142`,
  "Resource temporarily unavailable"), so these commits used `--no-verify`
  and I ran the same two scans by hand instead: no credential matches, no
  audio staged. Please make one ordinary commit from your own terminal to
  confirm the hook runs there.
- **Archived** `continue_v9/v10.py`, `rebuild_v9.py`, `events_v9*.json`,
  `run_v9/v10_variants.ps1` into `archive/` with a README.
- **Installer bug found and fixed:** its summary line printed a Unicode arrow
  and crashed with `UnicodeEncodeError` on a cp1252 console. Now ASCII.
- **Remote Script 1.7.1 deployed** to
  `C:\Users\Gebruiker\Documents\Ableton\User Library\...\AbletonMCP\__init__.py`,
  previous 1.7.0 backed up under a unique name.
- **Pre-flight verified end to end** (it correctly reports bridge unreachable,
  0 Live instances, 57.0 GB free with Live closed).
- **Bridge inventory** written to `docs/bridge-inventory-2026-09-18.md`.

## Blocked, needs you

1. **Live loads the OneDrive copy, and that copy cannot be written.**
   `Documents` is redirected to `OneDrive\Documents`, and the
   `__pycache__` there holds a `cpython-311` .pyc — Live's own interpreter —
   so the script Live loaded is the unpatched 1.7.0 mirror. Every write into
   that folder is denied (OneDrive/Defender protection on a known folder).
   Fix in Live: Preferences -> Library -> Location of User Library ->
   `C:\Users\Gebruiker\Documents\Ableton\User Library`, then restart Live.
   Full detail and the alternative in `docs/bridge-inventory-2026-09-18.md`.
2. **Start Live (Suite, not Intro)** with the AbletonMCP control surface
   selected, then run `python -m MCP_Server.producer.cli preflight` — it
   should show `script_version` 1.7.1 and a full v2 snapshot.
3. **Stale worktree** `.claude/worktrees/agent-issues-debug-dda3ed` still
   holds a copy at the old commit. `git worktree prune` does not remove it
   because the directory exists; `git worktree remove --force` deletes files,
   so I left that to you.
4. **`_to_delete/git-index.lock.empty`** is still there — an empty lock file
   I moved aside; deleting files on your machine was refused, so remove that
   folder yourself.
5. **The pending A/B verdict** (`cdf96e15…`) still needs your ears.
6. **The reference set** still needs the approval gate before any Suno v6
   download.
