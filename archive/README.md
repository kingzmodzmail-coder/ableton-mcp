# archive — parked one-off experiments

Versioned experiment scripts moved out of the repository root on 2026-09-18
(`continue_v9/v10.py`, `rebuild_v9.py`, `events_v9*.json`,
`run_v9/v10_variants.ps1`). Kept for history; not part of the producer path.

Superseded by `MCP_Server/producer/pipeline/` (stages + YAML section maps).
The one-off `_probe_*` / `_fix_*` scripts in `evaix-bridge/` are next in line,
but they were left in place pending Ryan's call.
